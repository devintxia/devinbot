// this is going to generate a sentence using a markov chain... hopefully

// const { spawn } = require('child_processs');

// const childPython = spawn('python', []);

// childPython.stdout.on('data', (data) => {
//     console.log(`output: ${data}`)
// })
